spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/departament_db
    password: '41494899'
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
  boot:
    message: Hello, Department Service Spring Cloud Updated
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    defaultZone: http://localhost:8761/eureka/

